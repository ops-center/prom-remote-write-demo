# prom-remote-write-demo

prometheus used `github.com/prometheus/prometheus/pkg/textparse` in scraping to extract samples. 
Here, we used `"github.com/prometheus/common/expfmt"` to extract sample from `MetricFamily`.
 